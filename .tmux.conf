# set-window-option -g mode-keys vi
#set-option -g history-limit 99999

# set-option -g status-bg black
# set-option -g status-fg red
#set-option -g status-position top

#set -g status-right "#[fg=brightgreen] \"#22T\" %Y.%m.%d %H:%M " # comment this line , then use show-options -g to find default setting
#set -g base-index 1
#setw -g pane-base-index 1

# move x clipboard into tmux paste buffer
# bind C-p run "tmux set-buffer \"$(xclip -o)\"; tmux paste-buffer"
#bind C-p run "tmux set-buffer \"$(cat /dev/clipboard)\""
# move tmux copy buffer into x clipboard
# bind C-y run "tmux save-buffer - | xclip -i"
#bind C-y run "tmux save-buffer - | cat > /dev/clipboard"

#bind k select-pane -U
#bind j select-pane -D
#bind h select-pane -L
#bind l select-pane -R

source-file ~/Shadow/dotfiles/dot.tmux.conf